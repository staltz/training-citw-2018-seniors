**Learn how to install native modules**

* Install `react-native-fs` from npm
* Set it up in your project
* Check that it's working: render the contents of `RNFS.DocumentDirectoryPath` (a simple string) on the screen
